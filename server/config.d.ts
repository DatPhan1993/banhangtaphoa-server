export declare const config: {
    PORT: string | number;
    port: string | number;
    NODE_ENV: string;
    environment: string;
    JWT_SECRET: string;
    JWT_EXPIRES_IN: string;
    DB_PATH: string;
    UPLOAD_PATH: string;
    CORS_ORIGIN: string;
    allowedOrigins: string;
};
